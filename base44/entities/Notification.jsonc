{
  "name": "Notification",
  "type": "object",
  "properties": {
    "wallet_address": {
      "type": "string",
      "description": "Wallet address of the notification recipient"
    },
    "type": {
      "type": "string",
      "enum": [
        "streak_milestone",
        "token_milestone",
        "viewer_streak",
        "system"
      ],
      "default": "streak_milestone"
    },
    "title": {
      "type": "string"
    },
    "message": {
      "type": "string"
    },
    "milestone": {
      "type": "number",
      "default": 0,
      "description": "Streak day count or token threshold that triggered this milestone notification"
    },
    "viewer_wallet": {
      "type": "string",
      "description": "For viewer_streak type: the wallet of the viewer who hit the streak (for shoutout linking)"
    },
    "read": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "wallet_address",
    "title",
    "message"
  ]
}