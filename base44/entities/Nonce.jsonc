{
  "name": "Nonce",
  "type": "object",
  "properties": {
    "nonce": {
      "type": "string",
      "description": "Unique challenge string signed by the wallet"
    },
    "session_id": {
      "type": "string",
      "description": "Tenant-scoped session id issued with the nonce; validated on verify to prevent cross-session spoofing"
    },
    "wallet_address": {
      "type": "string",
      "description": "Optional: wallet this nonce was issued to"
    },
    "expires_at": {
      "type": "string",
      "format": "date-time",
      "description": "When this nonce becomes invalid"
    },
    "consumed": {
      "type": "boolean",
      "default": false,
      "description": "True once a signature over this nonce has been verified"
    }
  },
  "required": [
    "nonce",
    "expires_at"
  ]
}