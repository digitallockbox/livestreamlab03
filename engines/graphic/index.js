const express=require('express');const app=express();const PORT=8000;app.get('/',(req,res)=>res.send('graphic engine online'));app.listen(PORT,()=>console.log('graphic engine running on port '+PORT));